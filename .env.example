DATABASE_URL="postgresql://evalkit:evalkit@localhost:5433/evalkit?schema=public"
SESSION_SECRET="replace-with-a-long-random-string"

# Optional: one-click recruiter login on the landing page
DEMO_LOGIN="true"

# Optional model providers. Without keys, evals use deterministic fixtures
# so the app still runs for demos.
# OPENAI_API_KEY=""
# GROQ_API_KEY=""
# OLLAMA_BASE_URL="http://127.0.0.1:11434"
